# Todo MVC Backend API
This is a typescript project which uses Mongo and Express for CRUD APIs.

## To run the project
- Run the command `docker compose build`
- Run the command `docker compose up -d`
